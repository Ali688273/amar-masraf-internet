pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // مخزن اختصاصی تپسل
        maven { url 'https://storage.tapsell.ir/sdk/maven' }
    }
}

rootProject.name = "amar-masraf-internet"
include ':app'
