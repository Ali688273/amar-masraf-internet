pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // مخزن جایگزین و بدون تحریم تپسل
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "amar-masraf-internet"
include ':app'
